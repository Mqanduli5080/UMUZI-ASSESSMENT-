# Example environment variables for backend

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE=sb_xxx (server-side only; keep secret)
# Optional: public/anon key if needed by frontend
SUPABASE_PUBLISHABLE_KEY=pk_xxx (frontend-safe)
