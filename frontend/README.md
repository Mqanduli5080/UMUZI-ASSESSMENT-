# Frontend

This directory contains the frontend code for the Coffee Brew App.

Place React components, pages/routes, client helpers, UI primitives, styles, and build configuration here.

Suggested structure:
- src/
  - components/
  - lib/
  - routes/ (or pages/ depending on framework)
- public/
- README.md
